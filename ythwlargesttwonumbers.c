#include<stdio.h>
#include<conio.h>

/*
yt-homework Q.1
	Write a program to find the largest of two numbers.
*/

int main(){
	int a,b;
	/*user input*/
	printf("enter the two numbers: ");
	scanf("%d %d",&a,&b);
	/*(isdigit) and if-else we are using*/
	if(a>b)
		printf("%d is the largest numbers.\n");
	
	else if(b<a)
		printf("%d is the larget numbers.\n");
	else
		printf("%d is the largest numbers.\n");	
	
	
	
	
	
		
}
